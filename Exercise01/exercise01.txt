#pragma omp barrier provides synchronization among all threads in the parallel section where all threads reach. As soon as any thread reaches the barrier, it will wait till all other threads reach the barrier. As soon as all threads have reached the barrier, only then the threads start executing. This means that all the thread# statements get executed prior to outside barrier statements.
